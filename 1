static void Add(string firstName, string lastName)
{
    int nextID = Math.Max(ActiveList.Concat(RemovedList).Max(p => p.ID), ID) + 1;
    Person user = new Person(nextID, firstName, lastName);
    ActiveList.Add(user);

    Console.WriteLine("\nPerson added successfully\n");
}
